{
  "name": "cloud-job-orchestrator-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json && node scripts/build.mjs",
    "check": "tsc --project tsconfig.json --noEmit",
    "test": "node --test tests/*.test.ts",
    "validate": "npm run check && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "typescript": "7.0.2"
  }
}

